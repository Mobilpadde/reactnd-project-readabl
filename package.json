{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "redux-thunk": "^2.2.0"
  },
  "scripts": {
    "front": "cd frontend && yarn start",
    "api": "cd api-server && yarn start"
  }
}
